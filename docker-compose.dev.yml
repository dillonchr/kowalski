
services:
  kowalski:
    env_file:
      - .env
    build: .
    command: ["npx", "nodemon", "index.js"]
    restart: on-failure
    volumes:
      - /Users/dillon/git/DATAS:/data
      - .:/code/
      - /code/node_modules/
